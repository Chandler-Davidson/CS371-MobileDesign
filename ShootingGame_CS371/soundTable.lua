local soundTable = {
    shootSound = audio.loadSound( "sounds/shoot.wav" ),
    hitSound = audio.loadSound( "sounds/hit.wav" ),
    explodeSound = audio.loadSound( "sounds/explode.wav" ),
}

return soundTable;
